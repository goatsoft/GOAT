# Public build-time config (no secrets here; secrets go in .env.local, which is git-ignored).
# CI overrides VITE_REPO and VITE_BASE from the GitHub context in .github/workflows/pages.yml.
VITE_REPO=goatsoft/GOAT
VITE_BASE=/
VITE_OMLX_URL=https://omlx.ai
